argo-workflows:
  dex:
    enabled: false
  server:
    extraArgs:
      - --insecure
    config:
      repositories: |
        - type: helm
          name: argo-workflows
          url: https://argoproj.github.io/argo-helm